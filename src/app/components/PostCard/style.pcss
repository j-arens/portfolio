.PostCard {
  @apply shadow border border-solid border-gray-900;
  border-width: 0.5rem;
}

.PostCard:hover {
  @apply bg-gray-900;
  color: #f8f8f2; /* foreground from dracula */
}

.PostCard__link {
  @apply block p-16;
}
