.container {
  @apply max-w-3xl m-auto;
}

.notifications {
  @apply mb-8;
  position: sticky;
  top: 2rem;
}

.title {
  @apply text-xl tracking-wide font-bold mb-8;
}

.group + .group {
  margin-top: 2rem;
}

.label {
  @apply mb-2 text-lg tracking-wide;
}

.label,
.input {
  display: block;
}

.input,
.submit {
  @apply border border-gray-900 leading-loose px-2 tracking-wide;
  border-width: 0.25rem;
}

.input {
  width: 100%;
  background: none;
}

.input:invalid {
  @apply border-red-500;
}

.input.hide-invalid {
  @apply border-gray-900;
}

.submit:disabled {
  cursor: not-allowed;
}

.submitting .submit,
.submit:hover {
  @apply bg-gray-900;
  color: #f8f8f2; /* foreground from dracula */
}

.submitSpinner {
  width: 1.5rem;
  height: 1.5rem;
  margin: 0.25rem 0.9rem;
}
